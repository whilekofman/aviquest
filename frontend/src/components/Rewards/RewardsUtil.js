import { useDispatch } from "react-redux";
import * as userActions from '../../store/user';


const dispatch = useDispatch


const rewardsUtils = (health) => {
    
    if (health <= 0){
    // console.log(health);
    }
}

export default rewardsUtils;